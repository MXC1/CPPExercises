#include <iostream>
#include <climits>

using std::cin;
using std::cout;

int main() {
    cout << sizeof(long long);
    // cout << LONG_MAX;
    cout << LLONG_MAX;
    cout << WINT_MAX;

    return 0;
}